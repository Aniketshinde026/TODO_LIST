// This is an optional file for measuring performance in the app
const reportWebVitals = (metric) => {
  console.log(metric); // or send it to an analytics endpoint
};

export default reportWebVitals;
